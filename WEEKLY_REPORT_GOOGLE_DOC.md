# 行動資安檢測平台 — 專案進度與架構重構報告

**報告日期**：2026 年 9 月 11 日  
**專案名稱**：Cloud-Based Mobile App Security Analysis (CMAA) & APP Crawler  
**報告主題**：因應 AWS Lambda 運算配額限制之「雲地混合微服務」架構重構與雙節點實體隔離實踐

---

## 摘要 (Executive Summary)

本週在推進 CMAA 行動資安雲端檢測平台之商業落地時，遭遇 AWS 原廠對新帳號無伺服器函式（AWS Lambda）記憶體配額上限之限制（預設僅 3,008 MB，而 Android APK 反編譯尖峰需求達 4~7.5 GB，導致 OOM 異常中斷，且配額調升申請遭原廠否決）。

為避免高昂之雲端重型算力帳單並實現系統自主可控，我們啟動「雲地混合（Hybrid Cloud）」架構重構：
1. **雲端輕量保留**：AWS EC2 僅承載前台 Web 與業務中樞（CMAA-Int），維持商業門面。
2. **算力回歸地端**：引進 Tailscale 零信任點對點加密穿透，在不更動學校防火牆與免開 Port 條件下，將靜態分析（CMAA-Astatic）對接至實驗室專用伺服器。
3. **雙節點物理隔離**：將「線上客戶即時檢測」與「大數據批次爬蟲長跑」分流至兩台實體伺服器，達成互不搶佔資源之生產級架構。
4. **歷史缺陷修復**：徹底定位並修正底層 Androguard 將檔名解碼為 `~)^` 之歷史漏洞。

---

## 一、 面臨挑戰：AWS Lambda 算力瓶頸與成本限制

原交接架構規劃將 Android 靜態分析 Worker 託管於 AWS Lambda，經實務部署後發現以下嚴峻挑戰：

1. **記憶體突發需求與 OOM (Out-Of-Memory) 硬傷**：
   - Android APK 逆向工程（Androguard）在解析大型 DEX 字串池、構建抽象語法樹（AST）時，記憶體消耗極大。
   - 實測數據：12 MB APK 反編譯尖峰耗用約 3.5 GB RAM；94 MB APK 尖峰耗用約 7.5 GB RAM。
   - AWS 新帳號預設 Lambda MicroVM 上限僅 3,008 MB。低於門檻時，AWS 系統直接發送 `SIGKILL` 終止程序，導致使用者端任務永久卡死。
2. **原廠配額審核未獲放行**：
   - 2026-09-09 依前案提出調升至 10,240 MB 之申請，遭 AWS 服務團隊以「新帳號缺乏常態基準用量」為由駁回。
   - 若改以大型 EC2 實例（如 16GB+ RAM）在雪梨節點全天候運行，每月將產生數百美元之固定雲端支出，不符經濟效益。

---

## 二、 因應架構重構：雲地混合與生產/批次雙節點實體隔離

針對上述限制，我們迅速啟動替代方案，並進一步將系統演進為高可靠度之分散式微服務架構：

### 1. 三端拓撲與職責劃分

| 實體節點 | 部署位置 / 硬體 | 承載模組與核心技術 | 職責與架構優勢 |
| :--- | :--- | :--- | :--- |
| **主機一：AWS EC2** | 澳洲雪梨區 (`t3.small`) | **CMAA-Int**<br>• React (Vite) 前端<br>• Node.js (Express) 後端<br>• Firebase 認證 / S3 簽章 | **SaaS 商業入口大腦**<br>僅維持輕量 Web 服務，不承擔任何吃重運算，永不當機且成本極低。 |
| **主機二：實驗室舊伺服器** | 學校實驗室 (`192.168.50.53`) | **CMAA-Astatic**<br>• Androguard 8010<br>• Queue Wrapper 5001<br>• Celery + Redis 任務佇列 | **線上客戶專屬檢測節點**<br>物理隔離專屬支援 EC2，保證外部付費客戶上傳 APK 享有 0 排隊、即時反編譯。 |
| **主機三：實驗室 5860 工作站** | Dell Precision 5860<br>(`192.168.50.120`) | **Mobile-APP-Crawler**<br>• 121 萬筆 PostgreSQL (5433)<br>• 9 組 Tor 代理池<br>• 獨立檢測與 PDF 產生器 | **大數據爬蟲與批次長跑節點**<br>專職 24 小時全自動爬取 Google Play，就算高負載運算亦絕不干擾線上客戶。 |

---

### 2. 關鍵工程突破

#### (1) 零更動校園防火牆之跨國安全穿透 (Tailscale Mesh VPN)
- **問題**：校園內網伺服器位於 NAT 防火牆後方，外網 AWS EC2 無法直接尋址，且向計中申請公開 Port 轉發流程冗長。
- **解法**：採用 WireGuard 協定之 Tailscale 建立虛擬加密私網，兩端點對點直連。實測雪梨 EC2 容器內部可無縫訪問地端 5001 Port，連通率 100%。

#### (2) 修復 S3 介面合約斷層
- **問題**：雲端後端（CMAA-Int）將 APK 存放於 S3 後，係以 JSON 格式發送 Object Key；而地端舊版 Queue Wrapper 僅支援 Multipart 二進位上傳，導致請求被以 400 錯誤拒絕。
- **解法**：重構 `wrapper.py`，新增 S3 Boto3 非同步拉取邏輯，地端接收任務後自動自雪梨 S3 串流抓取檔案，完美橋接前後端協定。

#### (3) 破解底層歷史亂碼 `~)^` 之工程根因
- **問題**：產出之資安診斷報告封面，檔案名稱固定退化為亂碼字元 `~)^`。
- **定位**：追查原始碼發現，歷史腳本在呼叫分析模組時固定傳入未編碼參數 `-n "file"`，而底層接收端執行了 `base64.b64decode("file")`。字串 `"file"` 經 Base64 解碼後，其二進位恰好對應 ASCII 字元 `~)^`。
- **修復**：於 Celery 任務層完成元數據回填，精準還原使用者上傳之原始檔名（如 `app-debug.apk`），消除歷史瑕疵。

---

## 三、 系統現況與全流程驗證成果

本週已於實體環境完成端到端（End-to-End）閉環驗證：

1. **上傳與排程**：使用者於前端網頁上傳 APK ➔ S3 儲存 ➔ EC2 發送分析通知 ➔ 地端舊主機接收並拉取。
2. **分析與計算**：Androguard 與 MalDroid 於地端容器內順暢執行 79 項特徵檢測，記憶體容限充裕，無 OOM 現象。
3. **報告生成**：順利產出結構化弱點 JSON 數據並生成完整 PDF 診斷報告。

---

## 四、 下一步工作規劃 (Next Steps)

1. **PDF 渲染模組回遷地端預先生成 (Pre-generation)**：
   - 目前架構係使用者點擊「Download PDF」時由 AWS Lambda 現時繪製，產生 20~30 秒延遲。
   - 下一步規劃將 PDF 產生器整合至地端檢測流程末端，分析完成時同步預產 PDF 並回傳 S3，實現終端使用者 **0 秒即時下載**。
2. **前端下載互動指示優化**：
   - 增加網頁按鈕狀態反饋（生成進度與防連點保護），提升系統整體互動體驗。

---

> **附錄：專案組態與文件連結**  
> 全域設定檔、主機帳密與容器連接埠備忘錄已同步登載於 GitHub 專案庫根目錄之 `SYSTEM_MEMO.md`。
